// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:9.2.1'
        classpath 'com.google.gms:google-services:4.4.4'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

subprojects { project ->
    project.afterEvaluate {
        if (project.hasProperty('android')) {
            if (project.tasks.findByName('unitTestClasses') == null) {
                project.tasks.register('unitTestClasses') {
                    dependsOn project.tasks.matching { it.name.contains('UnitTestSources') }
                }
            }
            if (project.tasks.findByName('androidTestClasses') == null) {
                project.tasks.register('androidTestClasses') {
                    dependsOn project.tasks.matching { it.name.contains('AndroidTestSources') }
                }
            }
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
