import CoreLocation

public struct OSGLOCManager {
    private let locManager: CLLocationManager
    
    public init(locManager: CLLocationManager) {
        self.locManager = locManager
    }
    
    
}
